FINAL STATUS: APPROVED after corrections and independent Astra XHIGH recheck.
Content review of clarity, terminology, worked examples, and mathematical correctness.
Browser verification is recorded separately below.

# Week 8 clarity and correctness audit

Reviewed the full revised `_includes/optimization/uke8.md` (1–1189) and `week8_setup.md` (1–10), before the planned setup-tab, JSXGraph and additional worked-example integration. Read-only review; no lesson content changed.

## Overall result

The revision is coherent and substantially meets the request. All four numbered experiments and all four Python demonstrations explain their purpose and what to inspect. Definitions, examples and exercises follow a connected progression: model → local candidate → existence → global guarantee. The nine paper exercises cover the stated learning goals and reuse the taught examples rather than forming disconnected additions. Python comments now explain the substantive operations.

## Required small fixes

1. **Add one fully worked numerical example of gradient, directional derivative and local change (around lines 214–226).** The concepts are defined, and a gradient formula appears later, but the directional derivative is never evaluated for a concrete point and direction. For example, for `phi` at `(0,0)`, explicitly calculate `grad phi=(-5,-5)`, choose `p=(1,0)`, obtain `grad phi dot p=-5`, and compare the predicted change for `h=(0.1,0)` (`-0.5`) with the exact change (`-0.485`). This also gives the user the requested small paper calculation and demonstrates what the approximation means. Define `phi` locally if using it before its current introduction, or choose a simpler already introduced function.

2. **Qualify the zero-eigenvalue statement (lines 366–369).** “En null egenverdi gir derimot ikke noe svar” is too broad after the discussion has extended to `n` variables: an indefinite Hessian can also have a zero eigenvalue and still imply a saddle. Use the precise statement already present in the main text: “Er Hessianen semidefinit med minst én null egenverdi, avgjør ikke andrederiverttesten typen.”

3. **Define the new coding term “boolsk” where it first occurs (around lines 883–884).** Add that it means `True`/`False` (sant/usant), with `allowed` being true exactly for admissible plans. This makes the explanation self-contained before the boolean masking and `np.where` operations. If arrays have not already been explicitly introduced in the visible Python setup, also gloss “array” as a table/vector of values.

## Low-priority clarity polish

- The production grid bounds `np.arange(51)` and `np.arange(101)` are safe but unnecessarily unexplained and loose. Prefer maxima derived from the model: `b <= 100/10 = 10`, `r <= floor(50/3) = 16`, with `np.arange(11)` and `np.arange(17)` and a comment about the excluded stop value. This is not a correctness defect.
- “Sadelpunkt” appears at line 267 before its definition at line 320. A short forward phrase or “andre slags kritiske punkter” would satisfy the user's strict definition-before-use preference without duplicating the later explanation.

## Verification

- Ran the common setup and all four demonstration cells successfully with NumPy, SciPy and Matplotlib using a noninteractive backend.
- Production: 156 admissible integer pairs; optimum `(8,14)`; profit 6850; 94 components; 50 hours.
- BFGS: `(0,0)` gives 0; `(2,2)` gives approximately `(1.9899,1.9899)` with 7.9597 and gradient norm `1.46e-6`; both return success.
- Open interval: minimum sampled values `.1`, `.01`, `.001`; maxima `.9`, `.99`, `.999`.
- Midpoints: parabola `0 <= 1`; quadratic `-5.625 <= -3.750`; wavy function `20.250 > .500`. Hessian eigenvalues approximately `1.38196601`, `3.61803399`, agreeing with the exact values.
- Solved the three existing code exercises in memory and ran their existing assertions in independent namespaces; all pass.
- Checked all nine paper-exercise answer fields and the mathematical claims/proofs. No wrong numerical answers or substantive mathematical errors beyond the zero-eigenvalue overstatement above.
- Setup visibility/autorun races, new applets, new worked examples and exercise-field rendering are explicitly left for the root agent's ongoing integration and subsequent recheck.

## Recheck: applets and worked examples

Reviewed the integrated Week 8 source through line 1344, including the visible Python setup, both JSXGraph sources and their surrounding explanations. Root is testing actual browser rendering and slider interaction.

**Resolved:** All three earlier required fixes are complete. The example `f(x,y)=x^2+2y^2` at `(1,-1)` correctly calculates gradient `(2,-4)`, directional derivative `-2` along `(1,1)`, predicted change `-0.2`, actual change `-0.17`, and the quadratic correction `0.03`. The direction need not be normalized because the text explicitly uses differentiation with respect to the step parameter. The zero-eigenvalue statement now specifies a positive semidefinite Hessian. Boolean values are explained before masking.

**Mathematical verdict:** Both applet families are correct. For the stated parameter range, `C_a` is convex exactly when `a >= 0`; the negative-parameter endpoints and midpoint supply a valid counterexample. The proof for nonnegative parameters checks the lower curve, side bounds and upper bound. For `f_a(x)=a*x^2`, the displayed difference is exactly `a*t*(1-t)*(x-y)^2`; the classification into strict convexity, a constant function and nonconvexity is correct. Both introductions tell students which control to vary and which geometric comparison to inspect. The numerical examples and subsequent general proofs provide sufficient worked algebra.

**One required sequence fix:** The function applet's status text and explanation use “strengt konveks” (reviewed lines 718 and 730) before the definition (line 739). Move the existing “Konveks er ikke det samme som strengt konveks” subsection to immediately before the function applet, after the ordinary convexity definition and one-test warning. Alternatively, move its defining sentence above the applet. No new exposition is needed.

**Two small wording improvements, not mathematical blockers:**

- The applet status strings currently read as “M er innenfor: området er konvekst” and “Midtpunktet under linjen: strengt konveks”. These are true for these specific families, and the text explicitly warns against inferring convexity from one successful test. Still, labels such as “a >= 0: dette området er konvekst” and “a > 0: denne funksjonen er strengt konveks” make the family-specific classification clearer and avoid suggesting a general implication from one midpoint.
- The overview still says “All Python ... ligger i 8.6”, although imports now live in the separate visible setup tab. Change “All Python” to “Pythonforsøkene” (and optionally point to Python-oppsett).

No additional required mathematical or teaching-content changes found. Existing numerical results and exercise answers remain unchanged. The longer widget field is retained only for the algebraic tangent expression; short numeric fields and HTML emphasis markers are present in the source as requested, with browser verification delegated to root.

## Final sequence recheck

Confirmed the strict-convexity subsection now appears before the function applet: definition and uniqueness caveat at reviewed lines 686–700, applet introduction at 702–707, classification later at 734–749. This resolves the last required sequence fix. **No required mathematical or clarity corrections remain in the reviewed Week 8 content.** The source is approved from this audit's perspective; root's browser/slider tests remain the separate runtime gate.
