name: Reingest

on:
  workflow_dispatch:

jobs:
  reingest:
    runs-on: ubuntu-latest

    steps:
      - name: Reingest temporarily disabled
        run: |
          echo "Reingest is currently disabled."
          echo "This workflow is kept as a manual placeholder."
          
#name: Re-ingest notes on push

#on:
#  push:
#    branches: ["main"]

#jobs:
#  reingest:
#    runs-on: self-hosted
#    steps:
#      - name: Re-ingest
#        run: touch /var/lib/rag/reingest.trigger

        
